CREATE TABLE sales (
    id          INT,
    product     VARCHAR(50),
    category    VARCHAR(30),
    price       NUMERIC(10,2),
    quantity    INTEGER,
    sale_date   DATE,
    region      VARCHAR(20)
);